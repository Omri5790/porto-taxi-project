# לטל ועינת — מה קורה בפרויקט ומה צריך מכם

עודכן: 28.8.2026 · ההגנה: יום שני 31.8

קודם כל:

```bash
git pull origin main
```

---

## מה השתנה עכשיו, בקצרה

שלב 3 נכתב מחדש מאפס. הגרסה הקודמת נראתה מרשימה אבל היו בה שתי בעיות
אמיתיות שבוחן היה מוצא:

1. **קוד ה־LSH וה־Count-Min לא השפיע על הפלט.** הם רצו, הדפיסו מספרים,
   והתוצאה הסופית חושבה בלעדיהם. זאת בדיוק הדרישה המרכזית של המטלה.
2. **קבצי התוצאות לא היו ניתנים לשחזור מהקוד שבריפו.** הרצנו את הקוד,
   קיבלנו מספרים אחרים.

לכן שניהם הוסרו במקום להיטלא. יש עכשיו שלוש שיטות שבאמת רצות, מבני נתונים
מקורבים שבאמת חוסכים עבודה, ו־31 טסטים.

**חשוב שתדעו את זה לפני ההגנה** — אם מישהו מכם ראה מספרים ישנים (למשל
"100 מסלולים מעל 40 ק"מ"), הם כבר לא קיימים ואסור לצטט אותם.

---

## איפה אנחנו עומדים באמת

| | מצב |
|:---|:---|
| שלב 1 — ניקוי ופיצ'רים | ✅ מוכן, עם דוח שאומר איזה כלל הוריד כמה נסיעות |
| שלב 2 — קידוד H3 | ✅ מוכן, רזולוציות 8/9/10 |
| שלב 3 — כריית מסלולים | ✅ הקוד מוכן ונבדק · ❌ **עוד לא רץ על נתוני פורטו** |
| ריצה בענן (Dataproc) | ❌ נתקענו על מכסות CPU — זה מה שנשאר |
| מצגת השוואת שיטות | ⚙️ נבנית אוטומטית מקובץ התוצאות |

**התיקייה `output/` לא מכילה שום תוצאה של שלב 3, וזה בכוונה.** המספרים
היחידים שיש בריפו נמצאים ב־`output/synthetic_run/` והם מנתונים מיוצרים
עם מסלולים ששתלנו מראש — זה מבחן דיוק, לא תוצאה. יש שם README שאומר את
זה במפורש. אל תזיזו אותם ל־`output/` ואל תציגו אותם כתוצאות פורטו.

---

## מה כל אחד מאיתנו חייב לדעת להסביר

ההגנה היא בעל פה והבוחן שואל את כולם. הבריף אומר במפורש שזה לא "רשימת
מכולת" — הוא בודק עומק. שלושתנו צריכים לדעת להסביר את **כל** מה שלמטה,
לא רק את החלק שלנו:

1. **למה בכלל מבני נתונים מקורבים?** התשובה היא לא "כי ביקשו". יש 1.71
   מיליון נסיעות; ספירה מדויקת של כל תת־רצף דורשת shuffle של עשרות
   מיליוני מפתחות ברשת. ה־Count-Min Sketch מזהה 99.93% מהרצפים כנדירים
   *לפני* ה־shuffle.
2. **למה דווקא Count-Min ו־Bloom ולא משהו אחר?** כי הטעות שלהם הולכת
   לכיוון הבטוח: שניהם אף פעם לא *מפספסים* משהו נפוץ, הם רק לפעמים
   מכניסים משהו נדיר בטעות. אז מה שהם פוסלים — פסול בוודאות.
3. **מה זה support ולמה הוא anti-monotone.** מסלול ארוך יותר לא יכול
   להיות פופולרי יותר מחלקיו. כל הסינון בנוי על זה.
4. **שלוש השיטות ובמה הן נבדלות** (LSH, Suffix Array, Growth) — לא רק
   השמות, אלא למה כל אחת מוצאת דברים אחרים.
5. **למה אין מסלולים באורך 40 ק"מ.** זו לא תקלה: נסיעה יכולה לעבור
   מסלול באורך L רק אם היא עצמה באורך L לפחות, והחציון בפורטו מתחת ל־4
   ק"מ. יש בקוד `length_ceiling` שמחשב את החסם הזה ומדפיס אותו.

**מה לקרוא:** `docs/stage3_algorithms.md` — זה ההסבר המלא. אחר כך
ה־docstrings ב־`scripts/stage3/`, הם כתובים כדי להסביר ולא רק לתעד.

---

## חלוקת עבודה מוצעת (תשנו כרצונכם)

**עומרי** — הריצה בענן: להעלות את ה־parquet של שלב 2, להריץ `STAGES=3`,
למשוך תוצאות, לבנות מהן את המפה והמצגת.

**טל** — לוודא שהקוד רץ אצלך מקומית ולהבין את שלב 3 לעומק:

```bash
python -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
pytest tests/ -q             # אמור להיות 31 passed
./run_local.sh --synthetic   # ~4 דקות, מריץ את כל שלב 3 בלי דאטהסט
```

הריצה הסינתטית שותלת עורקים באורך ידוע ואז בודקת אם האלגוריתם מצא אותם.
אם אתה מבין למה היא עוברת — אתה מבין את שלב 3.

**עינת** — שלבים 1 ו־2 והתשובות לשתי השאלות הראשונות של המטלה (אזורי
פעילות, ניתוח זמני). `docs/stage1_data_cleaning_report.md` מסביר את עשרת
כללי הניקוי; כדאי לדעת להגן על שניים מהם במיוחד — למה בודקים את תיבת
הגבול על *כל* נקודה ולא רק על הקצוות, ולמה משך הנסיעה מחושב מספירת
הדגימות המקורית ולא אחרי הסרת כפילויות (זה היה באג שניפח את המהירות
הממוצעת ב־23%).

---

## פרומפטים מוכנים לקלוד שלכם

העלו את הריפו (או את התיקייה) לשיחה, ואז:

**כדי להבין את שלב 3:**

> אני מגן בעל פה על פרויקט שמכרה מסלולים פופולריים מ־1.71 מיליון מסלולי
> מוניות בפורטו, עם PySpark. תקרא את `docs/stage3_algorithms.md` ואת
> `scripts/stage3/` ותסביר לי בשפה פשוטה: מה עושה כל אחת משלוש השיטות,
> למה השתמשנו ב־Count-Min Sketch וב־Bloom Filter ולא בספירה מדויקת, ומה
> זה anti-monotonicity. תעצור אחרי כל חלק ותשאל אותי שאלה כדי לוודא
> שהבנתי. אל תמציא מספרים — אם אתה לא מוצא מספר בקוד או בקבצים, תגיד
> שאין.

**כדי להתאמן על ההגנה:**

> תשחק בוחן קשוח בקורס מחשוב ענן. יש לך את הריפו. תשאל אותי שאלה אחת כל
> פעם, תקשיב לתשובה, ותגיד לי בכנות אם הייתי משכנע או מתחמק. תתמקד
> בשאלות "למה" ולא "מה": למה הרזולוציה הזו, למה השיטה הזו, למה התוצאה
> הזאת ולא אחרת.

**אם משהו לא רץ:**

> `./run_local.sh --synthetic` נכשל אצלי. הנה הפלט המלא: [הדבק]. תמצא את
> הסיבה. אל תשנה את האלגוריתמים כדי שהטסט יעבור.

---

## מה לא לעשות

* **אל תוסיפו מספרים שלא יצאו מריצה.** כל מספר במצגת ובמחברת נקרא
  אוטומטית מקבצי התוצאות, בדיוק כדי שזה לא יקרה. אם משהו חסר — הוא חסר
  כי לא הרצנו עדיין.
* **אל תחזירו את הקבצים שנמחקו.** `stage3_subroute_mining.py`,
  `stage3_popular_long_subroutes.py`, `gcp_run_proof.json` וכו' נמחקו
  כי הם או קוד מת או תוצאות לא ניתנות לשחזור.
* **אל תריצו קלאסטר ותשכחו אותו.** קלאסטר של 6 nodes ששכחנו רץ כמה ימים
  ואכל תקציב. `run_pipeline_dataproc.sh` מוחק את עצמו בסוף, וגם אם הוא
  קורס באמצע — יש `--max-idle`. אל תעקפו את זה.
* **אם `git status` מראה שינויים שאתם לא מזהים** — אל תעשו `checkout .`
  לפני שאתם שואלים בקבוצה.

---

## אם יצירת הקלאסטר נכשלת

`ZONE_RESOURCE_POOL_EXHAUSTED` זה לא באג בקוד, זה אומר שאין מכונות פנויות:

```bash
MACHINE=e2-standard-2 bash scripts/run_pipeline_dataproc.sh   # מכונות קטנות
ZONE=europe-west1-d   bash scripts/run_pipeline_dataproc.sh   # אזור אחר
```

לפני ניסיון חוזר, לוודא שלא נשאר קלאסטר ישן:

```bash
gcloud dataproc clusters list --region=europe-west1
```

ראו `QUICKSTART.md` לרצף המלא.
